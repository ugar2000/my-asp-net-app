# ML Asset Placeholders

The ML.NET service expects `digit-mnist.zip` (a trained ML.NET model) and `digit.onnx`
(the ONNX counterpart served to the browser) to live in this directory.
Drop your models here or adjust `MachineLearning` paths in `appsettings.json`.
